"use client";
import React from 'react';
import './loading.scss';

export type LoadingProps = {
	// types...
}

const Loading: React.FC<LoadingProps>  = ({}) => {
	return (
		<div className='loading'>hola</div>
	);
};

export default Loading;
